---
name: qwen-image-prompt-writing
description: 按官方 Prompt Enhancer（PE）契约写 Qwen-Image-2.1 提示词 —— 输出 rewritten_prompt + wh_ratio / ratio_follow 的 JSON 契约，而不是裸提示词。Use when 用户说「Qwen-Image-2.1」「qwen-image-2.1」「qwen 生图」「qwen 改图」「多图参考编辑」「把这张图改成…」「prompt rewrite」「PE 改写」「wh_ratio」「ratio_follow」，或用网关 qwen-image-2.1 档出图/改图，或要把一句粗糙需求扩写成该模型能吃的标准画质描述。
---

# Qwen-Image-2.1 提示词写作（复刻官方 PE 契约）

Qwen-Image-2.1 的官方 Prompt Enhancer 是两个微调过的 Qwen3.5-VL 9B 改写模型：`t2i` 一支、`edit` 一支，**各自独立的 checkpoint 与 system prompt，不可互换**。本技能不运行那两个模型，而是**照它们的契约手写同等产物**——把 PE 的输出契约变成可复用的写作规则。

产物的形态和其他提示词技能根本不同：交付的不是一段话，而是一个**结构**。

```
t2i  →  {"rewritten_prompt": "<长英文画面描述>", "wh_ratio": "3:2"}
edit →  {"rewritten_prompt": "<改写指令>", "wh_ratio": "", "ratio_follow": "<image1>"}
```

`wh_ratio` 与 `ratio_follow` **互斥**：一个有值，另一个必须是空串。

> 边界：本技能只负责**提示词构造**。真正出图走 `TextEncodeQwenImage21`（ComfyUI 原生节点）或网关的 `qwen-image-2.1` 档，执行面参考对应网关文档，这里不重复。

## 何时使用

- 用 Qwen-Image-2.1 出图，需要把用户的大白话扩写成模型能吃的标准描述。
- 多图参考编辑（换背景 / 换装 / 合成 / 合影 / 商品组合），需要精确改写指令。
- 用户已有提示词但效果偏（改得太猛连带变量、或改得太轻看不出变化），需要按「属性解耦」重排。
- 需要确定输出画幅该给比例还是跟随参考图。

**不适用**：非 Qwen-Image-2.1 的模型。Z-Image 走 `zimage-prompt-writing`，H3 视频走 `h3-prompt-writing` / `h3-playbook`。

### 这套契约换的是什么（A/B 实测口径）

同 seed / 同画幅 / 同步数，只换提示词的对照（同一句需求：26 词的直译版 vs 约 550 词的契约版）：
**两臂都出了能用的图**，因为题材先验够强时短提示词也能蒙对场景。

差别在**指定项落位**：契约臂 25 项具体指定里 20 项命中、4 项部分、1 项未现；而这些项在短提示词臂
**全部由模型自行决定** —— 没写数量，吊灯就只出一盏；没写动作形态，「煮面」就落到「筷子搅锅」；
写了「左手提网勺捞面、右手扶盖」，才逐项兑现。

⇒ 判据：**模型会替你猜对的（题材、氛围、整体风格）不必写满；模型不会替你猜的（数量、方位、
动作的具体形态、材质、配色分区）必须写。**

⇒ 词长不构成主要成本（步数与分辨率才是），**该写就写满**。

## 第一步：分模式（每次必过的开关）

| 判据 | 模式 |
|---|---|
| 没有输入图片，只有一句需求 | `t2i` |
| 有 1 张及以上输入图片 | `edit` |

`t2i` 模式**出现任何图片都是错误**，不是降级——给错模式等于跑了另一个任务。

两条支路的口径几乎不重叠，别混用：

| | t2i | edit |
|---|---|---|
| 视角 | **观察者报告**画面里有什么 | **操作者下达**改动指令 |
| 语言 | 描述恒为英文 | 跟随用户语言（见下文决策 A） |
| 体量 | 固定 ~20 句 / 400–500 词 | 按意图伸缩（见「意图分支」） |
| 语气 | 陈述、第三人称、可对冲 | 祈使、精确、**禁止对冲** |
| 画幅字段 | 只用 `wh_ratio` | `wh_ratio` / `ratio_follow` 二选一 |

---

## 模式 A — t2i：八步

`t2i` 把一句需求变成**约 20 句、400–500 词、每句约 25 词**的长英文段落，读起来像在完成品照片前做现场记录。

**这份体量是常量**：三词需求和三百词需求产出同样大小的描述。短 brief 意味着你要补出画面的大部分，而不是写更少。

1. **拆 brief**——分三堆：
   - **Fixed（必须原样活下来）**：每一串要在画面里显示的文字（逐字符照抄，含标点和空格，保留原脚本）、命名物体、数量、颜色、位置、给了的比例。
   - **Job 指令**（关于怎么做事，不是画面有什么）：`用双引号`、`不要硬边色块`、`4K无噪点`、`文字要清晰`。**默默服从，绝不写进描述**——描述陈述画面里有什么，从不陈述要做什么。
   - **Open（你自拟）**：其余全部。**短需求意味着你在编造大部分画面。**
2. **定画幅**——用户给了就用，否则按主体朝向取默认。规则见 `references/output-ratio.md`。**比例只活在 `wh_ratio` 字段里，正文永远不写比例/分辨率/像素数。**
3. **开篇句**——一句约 20 词，点名媒介 + 风格 + 主体 + 背景与调色板，通常还要带朝向。模板与词库见 `references/t2i-phrasing.md`。**媒介名词是唯一永不省略的部分。**
若画面需要明确「相机怎么拍」（景别 / 机位高度 / 视角角度 / 焦距），在同一句里一并交代，取值见 `references/composition-shot.md`（哪些已实测、哪些待测，该文件末尾有标注）。
4. **写前列清单**——先落两张单子再动笔：①每个元素及其位置（需要 **8–14 个方位短语，通常约 10 个**，必须触达角落、边缘、中心，不能堆在中间）②画面里每串可读文字，按阅读顺序。
5. **走画面**——按画面怎么被填满来选顺序：
   - **分区型**（海报 / 页面 / 界面 / 版式 / 多物件的宽场景）：背景及其承载表面 → 顶带 → 左 / 中 / 右（每区 1–2 句）→ 底带。**背景紧跟开篇句，不要留到最后。**
   - **单主体填满**（肖像 / 特写 / 单个物件）：背景及虚化程度 → 姿势与位置 → 头与脸 → 身体与各衣物表面 → 手持/接触物 → 边缘剩余。内部继续用方位短语。
   - **约三分之一的句子以方位短语开头**（`In the upper-left corner, …`），让读者始终知道在看哪里。
   - 保持**单段落**。**只有画面真是堆叠区域**（面板 / 卡片 / 分区 / 幻灯片）才分段，且**每区域一段**。海报、版式这类分区是「读的顺序」而非「堆叠的区块」，仍应是一段——把每句话都独立成段是常见偏离。
6. **排布文字**——画面里没有要读的文字就**跳过本步**；约三分之一的图没有任何可读文字，给它们编造招牌是错误。有则按阅读顺序交代每串的位置、外观、内容：`a bold black headline across the top reads "…"`。**渲染文本用直双引号、保留原脚本**（中/俄/韩/日/阿拉伯文各自保留）。给出字重、颜色、大小写、相对字号；换行说成 `a second line` 而不是在字符串里放真换行。**不该被读的**标记（远处招牌、玻璃后的标签、密排正文）要写成 blurred / indistinct / too small to read，**不要编字母**。图表和表格的轴标签、刻度、图例、系列、单元格数值同样是文字，要逐项写出。
7. **光照独立成句**——每张图都有光，描述必须显式交代：光源、方向、质量、留下的阴影与高光。`The lighting is …` 独立成句，或折进某个表面的句子里。**不能隐含。**
8. **收尾全景句**——`The overall composition …`（或 `The overall design / mood / palette …`），在**同一句里**覆盖平衡与对称、调色板、风格、氛围。**只写一句，后面不许跟第二个总结。**

### t2i 的语气纪律

| 做 | 不做 |
|---|---|
| 现在时、第三人称、陈述句 | `you` / `create` / `make sure` / `the AI should` |
| 定语色：`deep navy`、`muted olive`、`pale cream` | 裸色 `blue`（用户给了 hex 除外） |
| 给材质：`brushed metal`、`coarse linen`、`frosted glass` | 只给名词 `metal` |
| 枚举：`three books`、`a notebook or a tablet` | 概括：`several items`、`various decorations` |
| 年龄给生命阶段/十年代：`a teenager`、`in her thirties` | 具体岁数 |
| 物体按类别：`a silver laptop`、`a compact hatchback` | 品牌（用户点名除外） |
| 不确定就对冲：`appears to be`、`likely`、`suggesting` | 对用户固定的东西也含糊 |
| —— | 质量炸词： `masterpiece` / `8K` / `highly detailed` / `award-winning` |

**物理自洽**：阴影背离光源、反射匹配表面前方之物、相邻物体尺度一致。用户要了不可能的东西，就按画面所呈现的那样描述它，让其余场景保持自洽。

### 透明 RGBA（模型原生能力）

Qwen-Image-2.1 能直出透明背景。官方给的写法是把描述包起来——中间才是你的画面描述（是否必需见下）：

```
This is an RGBA format image with transparency. [your description]. The image has an alpha channel and a transparent background.
```

⚠️ **「必须存 PNG 否则丢 alpha」在本链路上是免检项**：实测 41 张（23 + 18）**全部是 RGBA PNG**，连不需要透明的也是 ⇒ 网关链路一律输出 RGBA PNG，自动满足，不必额外操心。这是模型的原生能力，不用任何后处理。

**实测两条（同 seed 同画幅同步数，只换提示词）**：

- **官方句式不是必要条件。** 12 词的 `a red apple on a transparent background, PNG, alpha channel`
  同样出了真 RGBA —— PNG `colortype=6`、透明区占 25%、边框 alpha≈0。
  ⇒ 触发透明的是「transparent background / alpha channel」这组词本身，句式不是开关。
- **官方句式的差别不在透明，而在主体控制。** 12 词版把视角交给模型去猜（出了俯视果顶、
  没有果梗与叶子、底部带一圈夸张的发光晕）；50 词版写了
  `resting at a slight angle` / `lit softly from the upper left` / `leaf attached to its stem`，
  这些逐项兑现。⇒ 与主结论一致：**契约锁的是主体描述，不是开关。**

⚠️ **透明区的 RGB 是噪声，不是干净底色**（实测呈紫色噪点，均值 `(163, 57, 205)`）。下游必须**靠 alpha 合成**，
别把 RGB 当背景色用；忽略 alpha 直接看 RGB 会看到噪点。模型会自带一层半透明的接触阴影，
这是加分项，不要写进提示词去禁止它。

---

## 模式 B — edit：六条

### 1. 两个独立的语言决策，不要混为一谈

**(A) 描述性散文的语言（双引号外的每一个词）**：用户指令是中文 → 用中文；英文 → 用英文；**任何其他语言（日/韩/法/西/泰…）→ 一律英文**。这条不可协商。

**(B) 将被渲染进画面的文字的语言（双引号内的内容）**，按严格优先级：
1. 用户给了确切文字，或指名了目标语言 → 照办。
2. 否则若输入图已有文字 → 用**图中文字的主流语言**，即使用户指令是另一种语言写的。
3. 否则（图里没文字，指令也没指名语言）→ 用**用户指令本身的语言**——包括日语、韩语、泰语、阿拉伯语，**不要强行转成英文**。

两条加固：所有渲染文字**必须单语**——引号内不混中英、不出双语对，除非用户明确要（实测：中文指令 → 中文单语命中，招牌 `LOE POIP` → 「深夜面馆」，**灯管笔触与配色保留、零混排、其余细节零改动**）；**体裁不覆盖输入语言**——「规格表 / 电影感数据文档 / 故事板 / 技术参数」这种观感靠版式和字体排印实现，不是把标签切成英文（标准单位和用户给的专有名词可留拉丁字母）。

> 典型误判：图是泰文为主，用户用英文要求加/改标题但没给词 → 渲染文字必须是**泰文**，而周围的英文描述（A）保持不变。

### 2. 最高原则：属性解耦，拉满

**只改用户点名的那一个属性，把它推到强烈且不可能误解的程度，其余一切按输入保真。**

两种失效是对称的：

- **泄漏（Leakage）**——碰了没点名的东西：一次锐化顺手重新调了色；一次放大顺手改了构图；一次换风格把脸带偏；一次换装掉了配饰；一次换背景「顺手」清理了没提到的东西。
- **欠编辑（Under-editing）**——改得太轻，观众会以为这就是没改的原图。

**保留锁的是内容，不是编辑强度。** 可识别性靠「点名哪些保持不变」买来，而不是靠按住效果不放。

⛔ **硬边界：这条链路做不到像素级局部编辑。** 实测：拿原图当参考图 + 要求「把车头朝向调正」→ **朝向确实改对，但树冠出现噪声化伪影、画面质感被毁**。⇒「只改一处、其余像素不动」在本链路上**不可实现**；需要局部精确改动时，把**要改的那张放 `image_1` 并重写整段描述**，而不是指望指令只作用在局部。

### 3. 什么该锚定，什么该决定

- **锚在图上看得见的东西上**。任何空间、色调、上下文断言都必须来自画面里确实可见者。不确定某个细节是否存在就删掉——保留元素放在更高的抽象层描述，总比编一个具体细节安全。
- **说「保留什么」但不重新画它**。未点名的内容按**类型、位置、角色**点名，倾向用一句总括保留条款，而不是逐帧走查。**写具体的外观 = 给模型的生成指令**——你把本想保留的东西描述得越具体，它越容易漂移。只对真正在改的东西、或为了区分相似物体时，才写具体外观。
- **身份是最硬的不变量**：人脸身份与使之可辨的个人配饰；商品的精确设计、标识与数量；输入的渲染媒介（照片 / 动漫 / 插画 / 素描 / 3D 渲染 / 绘画）——除非用户点名要动，否则全部存活。**身份来自参考图时，指向那张图，不要用文字描述五官**——文字描述会让模型重画并损耗相似度。实测：写五官 → 与参考脸**像素差 91.4 / 直方图相关 −0.168**、**人物被换成另一人**；只写「保持身份不变」→ **20.9 / 0.902**、个人特征（挑染）完整保留。**这是本契约里收益最大的一条。**
- **先消歧，再提交**。把模糊意图、不精确的空间指涉、未参数化的风格词变成具体可观察的东西；把抽象质量语言翻译成它所暗指的视觉属性。指令有歧义或自相矛盾时，选最合理的解读并**当作决定陈述**。保留用户自己的动作动词、空间关系和所述状态；用户要求保留的一切视为绝对。**创意性/物理上不可能的意图要保留，不要纠正。**
- **只做所问**。不加用户没要求的操作，不顺手清理没提到的缺陷、叠层或杂物——不管它们多显眼。当编辑涉及移除/移动/显露时，要对新暴露的区域说够多，让结果保持物理自洽。
- **画面里的文字是字面的**。凡输出中会出现可读文字，必须提交确切字符——每个元素、加引号、不许概括或缩略。**无法提交的文字就干脆不要加。** 字体排印和语言跟随输入所确立的（用户另有要求除外）。向外延展画布时，明确称其为 **outpainting**。

### 4. 意图分支：改写多少

- **改这张图**（局部物体/属性/背景、文字或 UI、质量或风格、视角或画布变换）→ **澄清并约束**：精确说清改什么，其余一律站定。
- **用这个主体做一张新图**（主体放入新场景、跨图合成、写真/海报/信息图）→ **主动构建**：把场景、光照、构图、版式做到专业水准。**按所问伸缩**：普通放置保持克制，风格化拍摄或出版级海报则完整搭出来。

### 5. 图片指代规则

- **N ≥ 2**：用 `<image1>`、`<image2>`… 指代，避免 `图1`、`第一张图`、`the first image`、`image A`。**这是 PE 的输入格式要求。**
  ⚠️ 但在**不跑 PE 的链路**上（如 ComfyUI-Roundabout 网关，文本由 `TextEncodeQwenImage21` 直接吃手写提示词）**它未被证实是功能性的**：实测 3 seed × 2 写法（`图1/图2/图3` vs `<image1>/<image2>/<image3>`）**语义绑定都正确**——服装色相 6/6 命中参考值、人物 / 场景 / 招牌全到位，写作方式间的差（`5.1–15.1`）**小于**组内 seed 差（`24.0–35.9`）。
  ⇒ **照写即可、零成本；但不要把它当成「写错就失效」的硬约束，也别据此断言「中文指代会让标签系统失效」。**
- **N = 1**：**不要用标签**，自然指代（`图像`、`图片中`、`the image`）。
- 必须**显式说明每张图的角色**——谁是画布（其构图与未点名内容存活），谁提供要迁移的素材——并说清从每张图取了什么。无画布的场景生成（合影/合照类）中，所有图都是身份来源。**逐张描述被引用的图，绝不把几张压缩成一个区间或一组来省事。**

### 6. 写作形态

**以操作开头，不是以成品画面开头**，从「只拿着输入图的人」的视角写。整段是**单个连续段落、无换行**。

- 要求用**肯定式**（`保持背景与输入图完全一致`）而不是禁令式（`禁止改变背景`）。`保持/保留[X]不变` 这种保留措辞是可以的。
- **精确果断：不对冲、不留未决的备选、不留悬空的程度词。**
- 所有会渲染成可见文字的内容放在双引号里；不渲染的描述性/结构性语言**不要**加引号。专有名词与领域术语保留原语言、放进英文双引号。
- **`rewritten_prompt` 里绝不出现任何比例或分辨率信息**（`2:3`、`16:9`、`1920x1080`、`2K`、`4K`）——那些只由 `wh_ratio` / `ratio_follow` 传达。
- 写完**最后做一次语言自检（B）**：重扫每个双引号串，不许中英混排、不许双语对、不许带括号注释翻译。

---

## 画幅落地（经 ComfyUI-Roundabout 网关调用时的关键约束）

`wh_ratio` / `ratio_follow` 的判定规则在 `references/output-ratio.md`。判完还得知道**这条链路怎么兑现**，否则两个字段是死的。

### 无参考图（纯文生）

canvas 来自 ResolutionSelector 的 `width` / `height`，直传，可非方图。**原生直出到 2K（`2048×2048`）**。

⛔ **画布边长必须落在 32 的整数倍上**，否则会被悄悄改写——同源同构图、只改裁出尺寸的三例探针：`672×1024`（21×32 / 32×32）**保留**、`640×960`（20×32 / 30×32）**保留**、`576×1008`（高为 **31.5×32**）被改写成 **`576×1024`**（宽保住、高被吸走）⇒ 算完宽高**先取整到 32 的倍数再传**。

**请求侧要传 `size: "832x1248"`**（`"WxH"` 字符串）。⛔ 顶层 `width` / `height` **不是可传参数**——传了会被**静默丢弃**，落回 defaults 的 `1024×1024`，不报错也不警告。这条安静得可怕：产物照出，只是画幅不对。

### 有参考图（编辑）

**默认 `custom_size` off：canvas 来自 `TextEncodeQwenImage21` 输出的 latent，也就是跟随 `image_1`。**

- 其余参考图（`image_2` 及以后）**可以与 `image_1` 尺寸、比例都不同**，不影响输出。
- ⛔ **只用文字声明画幅无效**：正文写「9:16，576×1024」输出仍是 `1024×1024`（画面与不写只差 `0.21×` 地板）⇒ 编辑档的画幅**只能靠画布图兑现**。
- 于是 `ratio_follow=<image1>` **天然兑现**，不用传任何参数。
- ⚠️ **硬推论：多图编辑时，被编辑的那张必须放在 `image_1`。** PE 判出的画布若是另一张，就把那张换到第 1 槽。`ratio_follow` 指到 `<image2>` 及以后时**不报错**——输出只是悄悄跟随了错误的那张。这是最难发现的一种错。

**`custom_size` on（用 `width`/`height` 覆盖 canvas）**：可以开，但取值必须**贴近 `image_1` 缩放后的结果**，否则**编辑内容会偏移**——同样不是报错，而是画面错位。

⛔ **有参考图时 `size` 不生效**：输出逐像素跟随 `image_1`（画布 `640×960` 就出 `640×960`）。网关的 `use_custom_size` 按「本次有没有参考素材」自动推导，**请求侧没有开关**。

#### 于是 edit 的 `wh_ratio` 非空时要另行兑现

PE 按「用户说了海报 / 横版」判出 `wh_ratio="2:3"`，**光靠这个字段本身兑现不了**。处置是：

> **把画布图预裁成目标比例再喂进去。** 画布 2:3，输出就逐像素是 2:3。
> **预裁时先把边长取整到 32 的倍数**——`832×1248` 之所以成立，是因为 832=26×32、1248=39×32（当年是撞对的）；不取整会被悄悄改写。

这样既不必开 `custom_size`，也**没有「取值偏离 `image_1` 导致编辑偏移」的风险**。契约里照常填 `wh_ratio`——它记录的是用户的意图与画幅决定；预裁是**喂图前的兑现步骤**，两者不冲突。另一条路是改网关让它暴露 `custom_size`，但偏移风险得自己扛。

### `resolution` 是面积基准，不是边长

**最容易读错的一个参数。** `resolution=1024` 的语义是**总像素约 `1024×1024`（1 兆像素）**，画面按参考图原始比例把这片面积分给宽和高；`2048` 则约 4 兆像素。

- 官方默认 `1024`，上限 `2048`（原生 2K）。⚠️ 该参数在网关的 `fields` 里 **0 次出现** ⇒ **未经网关暴露**，只能读节点层。
- **网关与官方模板的起始值是 `0`：不缩放，只对齐到 32 的倍数** —— 保留每张参考图的原始尺寸。

参考图的缩放与取整由节点自行处理，不必写进提示词。再重复一遍：**比例永远是字段，不是描述。**

## 其他链路事实与参考槽

- `TextEncodeQwenImage21` 才是 2.1 的节点（`comfy_extras/nodes_qwen.py`）。**`TextEncodeQwenImageEdit` / `…EditPlus` 是旧 Qwen-Image-Edit 的**，别挂错。
- **参考图数量**：官方口径一致是 **10 槽**（`image_1`–`image_10`），README 与 Comfy 官方模板都是 10；节点本身是 Autogrow `images.image_N`，技术上限更高；**网关 `qwen-image-2.1` 实际限制 6 张**。以链路实际为准。
  - **超限是 400 明确拒绝**（传 7 张 → `HTTP 400 … at most 6 image(s)`）——与 `width` / `height` 的静默丢弃相反，这条是**响的**，可以放心靠报错。
  - ⭐ **参考槽的边际收益在 3 张左右饱和**（同 prompt 同 seed，只改张数）：换 seed 的差异 1 槽 `33.69` → 3 槽 `23.52` → 6 槽 `26.20`（相对 1 槽 `0.70×` / `0.78×`）⇒ **3→6 不再下降，约束已饱和**。1→3 的变化是**新锚把它的具体内容照搬了进来**，属**素材注入**，不是保真提升。
    ⇒ **3 张足够钉住画面；第 4–6 张主要用于补齐素材，代价是耗时超线性增长**（成本面见「参数口径」）。
  - 附带：槽里放第二张人脸**不会串扰**（仍是单人物），只是人物更近、发色层次略多。
- ⚠️ **同参数重复请求会命中缓存（≈1.0s 返回）** ⇒ 做对比实验**必须换 seed**；要判复现**必须锁 seed**。

## 参数口径：`cfg` / 负向 / `steps`

`cfg` 与 `steps` 的暴露面比想象中窄：**只有 `generate_image` 暴露它们**，`edit_image` 的 MCP schema 里没有这两个参数（`get_tool_info` 说 `applies=true`，但**「可传」≠「每个工具面都暴露」**）⇒ 要扫或调这两个参数，走 `generate_image`。

### `cfg`：默认 `1` 就是最优值

默认 `cfg=1`，此时 **`negative_prompt` 整条不参与计算**——同 seed 同 prompt 加不加负向**逐像素相同**（配对差 mean `0.000` / max `0.0`），已 **5 次独立复现**（文生 3 次 + 编辑支 2 次）。

- **不是网关 bug，是公式必然**：噪声预测 `uncond + cfg·(cond − uncond)`，`cfg=1` 时退化成 `cond`，**负向项权重恰为 0**；门槛之上单调增强（配对差 `1.5 → 5.0`、`2 → 8.4`、`4 → 14.3`、`6 → 18.0`）。
- **抬 `cfg` 买不到兑现，只买到代价**：用一份含 **25 项可数指定**的契约式长描述扫 `cfg ∈ {1, 1.5, 2, 2.5, 3, 4}`，**逐项高倍对账的命中面几乎不随 `cfg` 变**（三本书／数量／配色分区／材质／方位／光照全档都在；唯一一致落空的是**动作形态**类，如「台灯臂折下」）；代价面单调变差，过曝像素 `0.035 → 0.269 → 1.085 → 1.870 → 3.141 → 4.034%`。
- ⇒ **默认 `cfg=1` 就是最优值；抬 `cfg` 的唯一正当理由是「需要负向生效」**（见下）。
  - ⚠️ **别把「`cfg=4` 会烧白」当普适结论**——过曝代价与**题材曝光**强相关：同一 `cfg=4`，夜景街道 **31.6%**、明亮桌面 **4.0%**。
  - ⚠️ **编辑支抬 `cfg` 的代价更贵，而且不是过曝，是取景**：脸部特写显示 `cfg` 1→4 时**主体逐级变大、肩部被裁出画面**——它偷偷改掉的是**取景**，而 edit 契约的核心承诺恰恰是保持未点名内容与构图不变。量级参考：编辑支 `cfg` 效应 单参考 `9.1–11.6` / **多参考只有 `3.1–5.6`（参考越多越抗 `cfg`）**；编辑支过曝 `1 → 0.08%`、`2 → 3.6%`、`2.5 → 7.4%`、`4 → 17.1%`。

### 负向：不是「删元素器」

**要排除一个元素，第一步是把它从正向提示词里删掉**——场景先验不会自己往空街上补车。

| 臂 | 正向怎么写 | 结果 |
|---|---|---|
| D1 | **完全不提车**（也不写「空」） | **空路** ← 主因 |
| D3 | 不提车 + 明写「路面完全空无一车」（3 seed） | 空路（与 D1 像素差 **16.6** ≈ 半个 seed 方差 ⇒ **冗余**） |
| D2 | **仍要车** + 又写「路面完全空无一车」 | **车与红巴士照出** ⇒ 矛盾时**肯定描述胜出** |

补一句「画面里没有 X」只是**冗余保险**；**若正向仍在要 X，补多少否定句都没用**。

⛔ **抬 `cfg` 也不等于能删元素**：`cfg=6`（负向权重 5）下写 `cars, vehicles, buses, traffic`，**车与红巴士照样在画面里**——正向用**同等强度（权重 6）**在要那些元素时，**明确写出的优先级最高**。**负向改的是采样轨迹，不是元素清单**（同例：`cfg=4` 加那串负向后过曝像素 `31.6% → 0.30%`，轨迹被整条换掉，元素却没删掉）。

⭐ **负向真正生效与否的判据是「正向有没有在要那个东西」**——它不是「删元素器」，而是**给一个正向没有主张的属性投票**：

- 文生支：正向**同等强度地在要**那些车（未删）⇒ 负向写 `cars, vehicles, buses`（`cfg=6`、权重 5）**删不掉**。
- 编辑支：正向**从没主张过**「换人」⇒ 负向写 `different person, changed face, deformed face` 在同一 `cfg` 内**确实生效**——与参考脸像素差 **21.37 → 10.02**（`cfg=2`）、**21.83 → 9.79**（`cfg=2.5`）。

⇒ **唯一可靠的负向用法：锚定「正向没有主张、但你不想它漂」的属性**（最典型 `different person, changed face` 用于身份保持）。**不要用它排除「正向正在要的元素」——那一定失败。** 且这么用要先把 `cfg` 抬起来，代价见上。

⚠️ **编辑支的负向参与度低于文生支**（`cfg=2` 时 `3.1–6.8` vs `8.4`），且单参考那列在 `cfg=4` **非单调回落**到 `2.34` ⇒ 参考图会压缩 `cfg` / 负向能搅动的空间。

### `negative_prompt` 是个死字段

契约自己规定它**恒为空串**（两个 PE 模型都不产负向），而链路默认 `cfg=1` 让负向**恒无效**——两件事**方向一致，不是矛盾**，不必为它做文章。结构事实（供排查）：它**确实被写进 `TextEncodeQwenImage21.negative_prompt` 并接进 `KSampler.negative`**，**不是被网关丢弃**，只是 `cfg=1` 下权重为 0。

✅ **加负向不拖慢生成**（可打消顾虑）：`cfg=1` 下有无负向输出逐像素相同 ⇒ 耗时差＝纯开销。实测 `134` / `838` / **`2718`** 字符的负向与不加负向成对 Δ 均值 `−0.007s ~ −0.05s`。

### `steps`：默认值与最优值是两件事

官方 pipeline 是 **euler 配 40–50 步**；Comfy 官方模板从 **25** 起步；网关该档默认 **40**。

| 用途 | 建议 steps | 理由 |
|---|---|---|
| 批量打样 / 挑构图 | **25** | 已过断崖，省 22–32% 耗时；此时真正要迭代的是 `seed`（见下方的量级对比） |
| **默认** | **40** | 落在官方 40–50 区间内；身份指标恰在此处到顶；0 参考时只要 **9.7s**，代价近乎为零 |
| 追求成片度 | **50** | 正向姿态执行最完整；再往上（60）无收益 |
| 下限 | **不低于 25** | **8 步不可用**：过曝 **6.926%**、服装色相偏到 **79.3°**（参考 72.9°）、高频低 18%、主体更小更远 |

**「40 是保守的默认值（就高不就低），不是测得的最优点。」** 平台从 25 就开始：多参考 3 张、同 seed、单变量下，与 `st40` 的配对差 `8 → 9.173`、`25 → 1.387`、`40 → 基准`、`50 → 0.819`、`60 → 1.131`——**`25→40` 的差比 `40→50` 还大，差值本身不单调** ⇒ 40 处的边际收益已在噪声级。

唯一支持抬到 40 的硬指标是单参考脸部**直方图相关**随步数单调改善（`0.642 → 0.797 → 0.902 → 0.920`，40 恰好在到顶处）；**引用它必须同时带上三条限制**：① 另一条指标「与参考的像素差」给的是**反方向**（`10.56@25` < `13.27@40`）⇒ 两条指标互相矛盾；② 直方图相关**混了影调**（曝光／明暗分布／内容占比都在里面），不能单独当「身份更像」的判据；③ **40 只有 1 个 seed**（25 与 50 各 3 个）⇒「40 是否最优」**未坐实**，能说的只有「25 之后进平台」。

**参考图在场时 `steps` 的真实边界**：断崖在 **8→25**（配对差 `9.173`，44% 像素 >8），之后是平的（`25→40/50/60` 只剩 `1.387 / 0.819 / 1.131`）。

**量级：`steps` 打不过 `seed`**——多参考 `25 vs 50` 效应 `8.320` vs 跨 seed 地板 `29.205`（比值 **0.28**）；单参考 `25 vs 40` 效应 `4.075` vs 地板 `10.096`（**0.40**）。⚠️ 且**效应本身看 seed 脸色**（同 `25→50` 跨度：`1.741 / 15.217 / 8.000`）⇒「多给步数能换来什么」**事前不可预期**。
⇒ **参考图在场时 `steps` 只影响「收敛是否充分」；要换姿态 / 构图请换 seed，不要指望加步数。**

**成本面**：多参考 ≈`0.48s/步`、单参考 ≈`0.23s/步`，近线性无拐点 ⇒ **25 步是性价比甜点**（≈60 步 59% 的耗时，效果已到顶）。3 参考 `27.8s`(40) vs `21.7s`(25)；**6 参考 × 40 步 = `103.0s`**（改用 25 省 ≈33s）。

⭐ **但参考图张数才是更大的杠杆**：同为 40 步，1 槽 ≈13s → 3 槽 ≈28s → **6 槽 ≈103s**（**超线性**），而第 4–6 张几乎只贡献素材、不提升保真 ⇒ **控成本要先砍张数，再谈步数。**

## 输出契约

给用户的交付：

- **t2i**：一段英文描述 + `wh_ratio`，配套说明画面取舍（8–14 个方位短语、光照句、收尾句是否都到位）。
- **edit**：一段改写指令 + `wh_ratio` 或 `ratio_follow`（二选一，另一个留空），配套说明——改了哪个属性、点名了哪些必须存活、画布图是哪张。
- 沿袭官方记录格式时，字段顺序稳定：`id` / `task` / `raw_prompt` / `input_images` / `task_type` / `thinking` / `positive_prompt` / `negative_prompt` / `wh_ratio` / `ratio_follow` / `parse_ok`。**`negative_prompt` 恒为空串**——两个 PE 模型都不产负向，字段只为给下游编辑器留槽位。

## 交付后怎么验收（可选闭环）

「可控」一半在写完时成立，另一半要**出图验证**。做一次就会建立判断力：

1. **出图前列指定项清单**——把这次**指定过**的每项逐条记下（数量、方位、动作形态、材质、排列、光色……各一行）。
2. **出图后逐条勾**：✓ 命中 / ~ 部分 / ✗ 未现。
3. **先扣掉先验自带的项**再算命中率——题材本来就有的东西（拉面店自带纸灯笼）不算你写对了。
4. **只看 ✗ 和 ~**：它们指向「哪个刻画面忘写了」，回到对应层次补。
5. ⛔ **量化代理必须先自证**：拿一个指标当判据前，先在**一对「有 / 无已知真值」的图**上试它——本轮两个自造代理**双双作废**：① 红像素占比（想抓红色双层巴士）——**没有巴士的臂也测出 0.26%**，被红砖建筑与信号灯污染；② 路面深色块占比——**空路 14.8% vs 有车 15.1%**，抓的是树影。通不过就废弃，**结论只以目视为准、并标明证据强度**。

⛔ 别用整体观感打分。「这张更好看」无法转成下一次怎么写；「第 14 项没兑现」可以。

⇒ 关键在**出图前**列清单。出图后凭记忆回想指定项，会自动把没写到的项脑补成写过了——
这与「把模型先验自带的元素算成自己的功劳」是同一个坑的两面。

## 常见坑

- **模式判错**：有图必走 `edit`。`t2i` 收到图是错误，不是警告。
- **把比例写进描述**：`wh_ratio` 是本契约唯一的比例出口，混进正文违反契约。
- **t2i 写太短**：三词需求不是写短描述的理由，而是补出整个画面的理由。
- **edit 出现质量炸词**：`8K`、`masterpiece` 属于泄漏——用户没点名要提升画质，它在 t2i 也是禁词，在 edit 更会顺带重画。
- **对保留部分写具体外观**：越具体越像生成指令，identity 越容易漂。
- **把「必须 `<imageN>`」当硬约束**：本网关上两种写法等价（见「图片指代规则」）。写 `<imageN>` 是零成本的好习惯，不是失效开关。
- **想靠负向排除元素**：先把它从正向删掉。正向还在要的东西，负向删不掉（见「参数口径」）。
- **为「用上负向」而抬 `cfg`**：抬 `cfg` 的唯一正当理由是让负向生效，而它只买到代价（过曝 / 取景变化）。
- **指望加步数换姿态 / 构图**：`steps` 打不过 `seed`，要换构图请换 seed。
- **`2K`/`4K`/`8K` 当比例信号用**：它们是质量描述符，不是宽高比指示词。
- **画布图没放第 1 槽**：见上文——这是最隐蔽的一个，表面看一切正常，只是输出跟随了错误的图。
- **用 `width`/`height` 传画幅**：这两个名字不在可传参数里，会被静默丢弃，产物照出但画幅是 defaults 的 `1024×1024`。**只有 `size` 有效。**
- **edit 时指望 `wh_ratio` 改画幅**：有参考图时 `size` 不生效，输出只会跟随 `image_1`；**正文里写「9:16」也没用**。要换画幅得**预裁画布图**，且边长取整到 32 的倍数。
- **t2i 分段过碎 / 句数不足**：官方量是**约 20 句、400–500 词、单段落**。只写十四五句、或把每句独立成段，都算没达标。
- **指望这套契约提画质**：它换的是**可控性**，不是画质。题材先验强时短提示词也能出好图；反过来，堆满词也救不回题材本身跑偏。
- **把模型先验自带的元素算成自己的功劳**：验收或做 A/B 时，先扣掉「这个题材本来就会出现的元素」（拉面店自带纸灯笼、森林自带雾气），只数由提示词**指定并兑现**的项，否则会把长提示词的价值算高。

## references 路由

| 情形 | 读哪里 |
|---|---|
| 判定 `wh_ratio` / `ratio_follow`（含场景语义表、扩图、全景、三视图） | `references/output-ratio.md` |
| 写 t2i 描述时缺词（开篇句模板、介质/风格名词、方位短语、色彩修饰、材质、对冲语、运动状态、排列、表面状态、尺度、遮挡、时间与天气） | `references/t2i-phrasing.md` |
| 要给画面定景别 / 机位高度 / 视角角度 / 焦距 / 景深 / 构图法则 | `references/composition-shot.md` |
| 出图执行、权重、参考槽接线 | 对应网关文档，不在本技能范围 |
